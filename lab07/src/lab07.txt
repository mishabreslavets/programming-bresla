Зміна лаби№6. Я переніс всю математичкну частину до функції.

З'явились труднощі, щодо передачі двовимірного масива до функції, тож я створив звичанйний масив, в який переніс вміст матриці, через  цикл.

Для корекного виконання програми, я створив ще один масив, одновимірний і пустий. У нього буде передаватись результат функції.

Для повернення до більш звичного виду, я створив ще одну змінну. Вона вже відвовідає за фінальний результат.

Тож, перши пункт виконано. 

Для розширення можливостей інпуту, а) я підключив бібліотеку stdio б) добавив цикл, що заповнює масив рандомними числами від 0 до 9 в) цикл, що перепризначеннає масив числами з консолі

Тобто, якщо користовуч не введе нічого, то програма виконається з рандомним інпутом.





Зміна лаби№5. З цією лабою все були вельми проще. Я просто переніс цикл до функції і добавив інпут з консолі
